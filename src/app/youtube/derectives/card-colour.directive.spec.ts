import CardColourDirective from './card-colour.directive';

describe('CardColourDirective', () => {
  it('should create an instance', () => {
    const directive = new CardColourDirective();
    expect(directive).toBeTruthy();
  });
});
